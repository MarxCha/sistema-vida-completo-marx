{
  "name": "sistema-vida-completo-marx",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && npx prisma db push && npx prisma db seed && npm run start",
    "build": "cd backend && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
